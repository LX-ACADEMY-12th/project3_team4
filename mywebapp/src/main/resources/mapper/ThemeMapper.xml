<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.team4.mywebapp.mapper.ThemeMapper">

<!-- 전체 테마 조회 -->
<select id="getAllThemes" resultType="com.team4.mywebapp.dto.ThemeDto">
	SELECT
		theme_id as themeId
		, name
		, category
		, style_info as styleInfo
		, minihome_minihome_id as minihomeMinihomeId
	From theme
	ORDER BY theme_id ASC
</select>

<update id="updateTheme" parameterType="ThemeDto">
	UPDATE theme 
	SET
		category = #{category}
		, name = #{name}
		, style_info = #{styleInfo}
	WHERE theme_id = #{themeId} 
</update>

</mapper>
