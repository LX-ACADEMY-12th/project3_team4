<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.team4.mywebapp.mapper.SignupMapper">

	<insert id="insertUser" parameterType="com.team4.mywebapp.dto.SignupDto">
		INSERT into user (
			login_id
			, login_pw
			, nickname
		)
		VALUES (
			#{loginId}
			, #{loginPw}
			, #{nickname}
		)
	</insert>
	
	<select id="checkDuplicateId" parameterType="String" resultType="int">
		SELECT 
			count(*)
		FROM
			user
		WHERE
			login_id = #{loginId}
	</select>


</mapper>