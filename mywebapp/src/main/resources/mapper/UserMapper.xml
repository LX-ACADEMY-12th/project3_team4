<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper.UserMapper">

	<!-- 회원가입 -->
    <insert id="insertUser" parameterType="UserDto">
        INSERT INTO users (username, password, nickname, gender, profile_link, created_at) 
        VALUES (#{username}, #{password}, #{nickname}, #{gender}, #{profileLink}, NOW())
    </insert>
    
    <!-- 프로필 업데이트 -->
    <update id="updateUserProfile" parameterType="UserDto">
        UPDATE users 
        SET nickname = #{nickname}, 
            profile_image = #{profileImage},
            status_message = #{statusMessage},
            updated_at = NOW()
        WHERE user_id = #{userId}
    </update>
    
    <!-- 사용자 삭제 -->
    <delete id="deleteUser">
    	delete from user where user_id = #{userId}
    </delete>
    
    <!-- 닉네임 중복 체크 -->
    <select id="checkNicknameExists" parameterType="String" resultType="int">
        SELECT COUNT(*) FROM users WHERE nickname = #{nickname}
    </select>
    
    <!-- 사용자명으로 조회 -->
    <select id="selectUserByUsername" parameterType="String" resultType="UserDto">
        SELECT user_id, username, password, nickname, gender, profile_link, 
               profile_image, status_message, created_at, updated_at
        FROM users 
        WHERE username = #{username}
    </select>
    
    <!-- ID로 사용자 조회 -->
    <select id="selectUserById" parameterType="int" resultType="UserDto">
        SELECT user_id, username, password, nickname, gender, profile_link, 
               profile_image, status_message, created_at, updated_at
        FROM users 
        WHERE user_id = #{userId}
    </select>
    
    
  
    
    
    



</mapper>